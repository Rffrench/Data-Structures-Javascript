// Nodes of Linked List

class LinkedListNode {
    constructor(value, next) {
        this.value = value; // This will hold the value of the node
        this.next = next; // THis will hold the value of the next node
    }
}

module.exports = LinkedListNode // Exporting the class